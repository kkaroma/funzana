<p style="color: green"><%= notice %></p>

<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= link_to category.name, category %><br />
  <% end %>
</div>

<%= link_to "New category", new_category_path, class: "btn btn-primary" if user_admin %>
